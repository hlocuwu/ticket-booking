apiVersion: v1
kind: Service
metadata:
  name: opensearch-dashboards
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: opensearch-dashboards
  ports:
    - port: 5601
      targetPort: 5601
      name: http
