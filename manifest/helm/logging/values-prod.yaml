opensearch:
  replicas: 1
  storage: 50Gi
  javaOpts: "-Xms2g -Xmx2g"
  adminPassword: ""   # set via ArgoCD parameter, do not commit real value

opensearchDashboards:
  ingress:
    host: opensearch.example.com   # replace with actual domain

tls:
  enabled: true
