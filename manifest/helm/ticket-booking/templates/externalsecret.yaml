{{- if .Values.externalSecrets.enabled }}
{{- if .Values.externalSecrets.createClusterStore }}
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: gcp-secret-manager
spec:
  provider:
    gcpsm:
      projectID: {{ .Values.externalSecrets.gcpProjectId | quote }}
      auth:
        workloadIdentity:
          clusterLocation: {{ .Values.externalSecrets.clusterLocation | quote }}
          clusterName: {{ .Values.externalSecrets.clusterName | quote }}
          serviceAccountRef:
            name: external-secrets
            namespace: external-secrets
---
{{- end }}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: ticket-secrets
  namespace: {{ .Values.namespace }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-secret-manager
    kind: ClusterSecretStore
  target:
    name: ticket-secrets
    creationPolicy: Owner
  data:
    - secretKey: db-password
      remoteRef:
        key: ticket-booking-db-password
    - secretKey: jwt-secret
      remoteRef:
        key: ticket-booking-jwt-secret
    - secretKey: smtp-username
      remoteRef:
        key: ticket-booking-smtp-username
    - secretKey: smtp-password
      remoteRef:
        key: ticket-booking-smtp-password
    - secretKey: sender-email
      remoteRef:
        key: ticket-booking-sender-email
    - secretKey: momo-access-key
      remoteRef:
        key: ticket-booking-momo-access-key
    - secretKey: momo-secret-key
      remoteRef:
        key: ticket-booking-momo-secret-key
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: ghcr-pull-secret
  namespace: {{ .Values.namespace }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: gcp-secret-manager
    kind: ClusterSecretStore
  target:
    name: ghcr-pull-secret
    creationPolicy: Owner
    type: kubernetes.io/dockerconfigjson
    template:
      type: kubernetes.io/dockerconfigjson
      data:
        .dockerconfigjson: |
          {"auths":{"ghcr.io":{"username":"hlocuwu","password":"{{ "{{" }} .token {{ "}}" }}","auth":"{{ "{{" }} list "hlocuwu" .token | join ":" | b64enc {{ "}}" }}"}}}
  data:
    - secretKey: token
      remoteRef:
        key: ticket-booking-ghcr-token
{{- end }}
