FROM python:3.14-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
ENV PORT=8086
EXPOSE 8086
CMD ["sh", "-c", "gunicorn --workers 2 --bind 0.0.0.0:${PORT} main:app"]
